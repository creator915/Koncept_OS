/**
 * updateGame: Main per-frame game logic update.
 *
 * @param {{world: object, player: object, enemies: Array, particles: Array, time: object, input: object}} state
 * @returns {{world: object, player: object, enemies: Array, particles: Array, time: object}}
 *
 * @example
 * updateGame({world:{...},player:{...},enemies:[],particles:[],time:{...},input:{...}})  // → updated state
 */
function updateGame(state) { throw new Error("updateGame: contract-only; implement in K/frags/UpdateGame.js"); }
