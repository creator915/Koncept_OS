/**
 * spawnEnemies: Spawn enemies based on time and player position.
 *
 * @param {{time: object, player: object, world: object, enemies: Array, spawnCap: number}} spawnContext
 * @returns {{enemies: Array}}
 *
 * @example
 * spawnEnemies({time:{isDaytime:true},player:{x:210,y:80},world:{...},enemies:[],spawnCap:20})  // → {enemies:[...]}
 */
function spawnEnemies(spawnContext) { throw new Error("spawnEnemies: contract-only; implement in K/frags/SpawnEnemies.js"); }
