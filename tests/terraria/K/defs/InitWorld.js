/**
 * initWorld: Generate simplified Terraria world terrain.
 *
 * @param {{width: number, height: number, seed: number}} config
 * @returns {{width: number, height: number, tiles: Array<Array<{type: number, hp: number, wall: number, isActive: boolean}>>, spawnX: number, spawnY: number, seed: number}}
 *
 * @example
 * initWorld({width:420,height:240,seed:12345})  // → {width:420,height:240,tiles:[...],spawnX:210,spawnY:...}
 */
function initWorld(config) { throw new Error("initWorld: contract-only; implement in K/frags/InitWorld.js"); }
