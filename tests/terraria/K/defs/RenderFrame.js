/**
 * renderFrame: Render one frame to canvas.
 *
 * @param {{ctx: object, world: object, player: object, enemies: Array, particles: Array, time: object, input: object, camera: {x: number, y: number}, canvasWidth: number, canvasHeight: number}} renderState
 * @returns {void}
 *
 * @example
 * renderFrame({ctx,world,player,enemies:[],particles:[],time,input,camera:{x:0,y:0},canvasWidth:800,canvasHeight:600})
 */
function renderFrame(renderState) { throw new Error("renderFrame: contract-only; implement in K/frags/RenderFrame.js"); }
