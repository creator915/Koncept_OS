# Protocol docs

Currently the canonical protocol is in `internal/domain/protocol/protocol.go`.
Run `kcpos doc protocol` to render the markdown.

This directory is reserved for external-facing protocol documentation
(spec format conventions, integration guides) that don't fit in the
code-derived doc.
